async function onLoad() {
  // All visible UI is contributed declaratively: themes and the sandboxed
  // Settings destination are registered by the host from manifest.json.
}

module.exports = { onLoad };
